// import { redirect } from '@sveltejs/kit';

// export async function load({ locals }) {
// 	// This assumes you've set up auth in hooks.server.js to put session in locals
// 	// If you're not using server-side auth yet, you can add this later
// 	const session = locals.session;

// 	if (!session) {
// 		throw redirect(302, '/login');
// 	}

// 	return {
// 		user: session.user
// 	};
// }
